# AirGap Ethereum signer

A pure javascript ethereum signer client side offline app. Easy to review. Uses on purpose CSS only framework. Gets build with browserify.

# INSTALL
`npm install`

# BUILD
`npm run build`

# DEVELOP
`npm run watch`